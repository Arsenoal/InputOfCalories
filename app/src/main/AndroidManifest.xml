<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inputofcalories">

    <application
        android:name=".presentation.InputOfCaloriesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.regularflow.home.RegularUserHomeActivity"></activity>
        <activity android:name=".presentation.auth.signin.SignInActivity" />
        <activity android:name=".presentation.auth.registration.RegisterUserActivity" />
        <activity android:name=".presentation.regularflow.addmeal.AddMealActivity"/>
        <activity android:name=".presentation.regularflow.viewmeal.ViewMealActivity"/>
        <activity android:name=".presentation.regularflow.editmeal.EditMealActivity"/>
        <activity android:name=".presentation.managerflow.home.ManagerUserHomeActivity"/>
        <activity
            android:name=".presentation.launch.LauncherActivity"
            android:windowSoftInputMode="stateVisible|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>